info:
  name: Delete a pet
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseURL}}/pet/123456789"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create a new pet in the store
