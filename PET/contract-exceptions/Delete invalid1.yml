info:
  name: Delete invalid1
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseURL2}}/pet/123456789"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: petId
      value: ""
  scripts:
    - type: tests
      code: |-
        const responseBody = res.getBody();

        test("Status code deve ser 200 Not Found na deleção do pet", function() {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create a new pet in the store
