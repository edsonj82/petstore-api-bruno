info:
  name: Validate pet by ID deleted
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseURL}}/pet/{{petId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Multiple status values can be provided with comma separated strings.
