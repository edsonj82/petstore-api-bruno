info:
  name: Untitled
  type: http
  seq: 1

http:
  method: GET
  url: https://petstore3.swagger.io/api/v3/pet/findByStatus?status=available
  params:
    - name: status
      value: available
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
